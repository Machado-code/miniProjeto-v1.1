@model miniProjeto.Data.Categoria

@{
    ViewData["Title"] = "Detalhar";
}

<h1>Details</h1>

<div>
    <h4>Categoria</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Nome_categoria)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Nome_categoria)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.Id">Editar</a> |
    <a asp-action="Index">Voltar Para Lista de Categorias</a>
</div>
